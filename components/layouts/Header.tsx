import React, { memo } from "react";

type Props = {};

// eslint-disable-next-line react/display-name
export const Header = memo(() => {
    return (
        <>
            <div>
                aa
            </div>
        </>
    )
});
